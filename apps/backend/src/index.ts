/**
 * SiloBreaker Backend - Apollo Server + Neo4j
 *
 * Entry point for the GraphQL API server.
 * Implementation will be added in Phase 1.
 */

export function placeholder(): string {
  return 'SiloBreaker backend - Phase 1 coming soon';
}
